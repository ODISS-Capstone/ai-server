# Leave empty when the web app is served by FastAPI at the same origin (/app).
VITE_API_BASE_URL=
# Optional admin fallback. Normal testers should enter token at runtime or use ?token=.
VITE_MEMORY_BROWSER_TOKEN=
