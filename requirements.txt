# ODISS 서버엔진 — OCR 기반 멀티모달 시니어 복약지도 서버
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6
pydantic>=2.5.0
pydantic-settings>=2.1.0
websockets>=12.0

# HTTP client
httpx>=0.26.0
pytest>=8.0.0

# Local STT (Whisper Large-v3 via CTranslate2)
faster-whisper>=1.1.0

# Push notifications (FCM) — 앱 종료 상태 촬영모드/알림 전송 (선택적)
firebase-admin>=6.5.0
